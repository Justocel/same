# `make migrate` sources this file as a shell script, so quote any value
# containing spaces or shell metacharacters, e.g. PASS='p@ss word&'.
# Con auth local "peer" (usuario del SO) usá el socket: postgresql:///same
DATABASE_URL=postgresql://localhost:5432/same
LOG_LEVEL=INFO
